# readthedocs.yml

version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  install:
    - method: pip
    - requirements: docs/requirements.yaml
    - path: .

sphinx:
  configuration: docs/conf.py

conda:
  environment: docs/requirements.yaml
